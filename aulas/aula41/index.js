// Escreva uma função que recebe 2 números e retorne o maior deles

function getMax(x, y) {
    return x > y ? x : y;
}

const writeMax = (x, y) => x > y ? x : y;

console.log(getMax(50, 10));
console.log(writeMax(50, 10));