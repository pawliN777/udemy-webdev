// splice(índice, delete, elmento1, elemento2, elemento3);

const names = ['Paulo', 'Brendhon', 'Henriques', 'Oliveira'];

const removedNames = names.splice(-2, 0, 'Saturno');


console.log(names, removedNames)